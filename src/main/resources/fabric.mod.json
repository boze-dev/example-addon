{
  "schemaVersion": 1,
  "id": "example-addon",
  "version": "${version}",

  "name": "Example Addon",
  "description": "An example addon for the Boze Addon API",

  "license": "GPLv3",

  "environment": "client",
  "mixins": [
    "example-addon.mixins.json"
  ],

  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}"
  }
}
